V0.6.14
- Dice sound rebuilt for iOS/PWA: explicitly resumes AudioContext before roll and uses stronger multi-impact dice SFX.
- Dice animation remains intact and runs after sound initialization.
- Venture marketplace visual fallback no longer shows a generic building emoji when a logo is unavailable.
- Supplied/embedded logos use contain sizing; brands without an embedded logo use a clean branded initials card instead of pretending a logo exists.
- Cache bumped to force GitHub Pages/PWA update.
IMPORTANT: real brand logos must exist as bundled assets/data URIs to display. A browser cannot reconstruct logos from screenshots from an earlier ChatGPT turn unless those logo files were actually bundled into the project.
